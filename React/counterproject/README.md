1. Why we need React
   -> Efficent dom manipulation , Reusable Components , 
2. Why we need bundler ? 
 -> Bundler create a environment where all the things/ dependecy are present 
 which are need to built a react code 
3. JSX -> It is HTML look like syntax in which we can write JS logic 
4. Componnet-> A function which return JSX is called component 
5. 
